# author name
author: claudeliang

# the path where note files store
notepath: "/Users/claudeliang/.notes"

# the lable to quit input
quitlabel: "push"

# the label to new line
newlinelabel: "new"

# log level
level: "INFO"